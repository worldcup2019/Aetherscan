Flask>=3.0
requests>=2.31
beautifulsoup4>=4.12
google-genai>=1.0
PyQt6>=6.10
PyQt6-WebEngine>=6.10
# ^ 6.10+ specifically required for Chrome extension support
#   (QWebEngineExtensionManager). Older versions still run everything
#   else fine -- the Extensions button just stays hidden.